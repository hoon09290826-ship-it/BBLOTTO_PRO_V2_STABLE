# BBLOTTO PRO V2 STABLE RC7-17

## 문자간다 줄바꿈 최종 패치

- 구형 `.xls` 업로드에서 셀 내부 줄바꿈이 삭제되는 문제 대응
- 문자간다 직접 붙여넣기와 같은 LF(CHAR(10)) 줄바꿈을 `.xlsx` 셀에 저장
- 다운로드 파일 확장자를 `.xlsx`로 변경
- 추천번호는 `1)3,18,21,30,41,42` 형식으로 1줄 1조합 유지

## 적용 파일

- `backend/app.py`
- `frontend/app.js`
